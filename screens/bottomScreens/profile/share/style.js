import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    textAlign: 'left',
    padding: 5,
    fontSize: 17,
    color: 'black',
    left: 15,
  },
});

export default styles;
